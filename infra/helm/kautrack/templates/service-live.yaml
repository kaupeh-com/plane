apiVersion: v1
kind: Service
metadata:
  name: kautrack-live
  labels:
    {{- include "kautrack.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.live.port }}
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: kautrack-live
