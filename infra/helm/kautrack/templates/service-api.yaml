apiVersion: v1
kind: Service
metadata:
  name: kautrack-api
  labels:
    {{- include "kautrack.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.port }}
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: kautrack-api
