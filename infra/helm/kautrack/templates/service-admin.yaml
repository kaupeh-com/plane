apiVersion: v1
kind: Service
metadata:
  name: kautrack-admin
  labels:
    {{- include "kautrack.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.admin.port }}
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: kautrack-admin
