apiVersion: v1
kind: Service
metadata:
  name: kautrack-web
  labels:
    {{- include "kautrack.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.web.port }}
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: kautrack-web
