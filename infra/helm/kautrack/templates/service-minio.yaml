apiVersion: v1
kind: Service
metadata:
  name: kautrack-minio
  labels:
    {{- include "kautrack.labels" . | nindent 4 }}
spec:
  type: ExternalName
  externalName: minio.minio.svc.cluster.local
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP
